import { ProfileView } from "@/components/profile/profile-view";

/** Your own profile (reached from the Portfolio header avatar). */
export default function OwnProfile() {
  return <ProfileView variant="own" />;
}
